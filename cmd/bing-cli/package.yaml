name: bing-cli

ghc-options:
  -Wall

library:
  source-dirs:
    - src

executables:
  bingcli:
    main: cmd/Main.hs
    dependencies:
      - bing-cli

dependencies:
  - base
  - aeson
  - base-compat
  - bytestring
  - text
